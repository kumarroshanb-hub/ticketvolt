// src/components/Common/PageSubtitle.js
import styled from 'styled-components';
import { Typography } from '@mui/material';

const PageSubtitle = styled(Typography)`
    color: #64748b;
    font-size: 0.875rem;
    margin-top: 4px;
`;

export default PageSubtitle;