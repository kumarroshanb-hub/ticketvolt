// admin-frontend/src/components/Common/StatItem.js
import React from 'react';
import { Box } from '@mui/material';

const StatItem = ({ children }) => (
    <Box>{children}</Box>
);

export default StatItem;